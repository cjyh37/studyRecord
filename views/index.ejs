<div class="bg-white shadow rounded-lg p-6">
  <h1 class="text-2xl font-bold mb-4">온라인 학습관리스템에 접속하신 것을 환영합니다</h1>
  <p class="text-gray-600">학습을 시작하려면 강의를 선택하세요:</p>
  <div class="mt-8 space-y-2">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <% lectures.forEach((lecture)=> { %>
        <div class="bg-white rounded-lg shadow-md overflow-hidden">
          <div class="relative">
            <a href="/lecture/<%= lecture._id %>">
              <img class="w-full h-48 object-cover" src="<%= lecture.thumbnailUrl %>" loading="lazy"
                alt="<%= lecture.title %>">
            </a>
            <div class="absolute bottom-0 right-0 bg-blue-500 text-white px-2 py-1 rounded-tl-lg">
              <%= formatDuration(lecture.duration) %>
            </div>
          </div>
          <div class="p-4">
            <h2 class="text-xl font-bold text-gray-800 mb-2">
              <%= lecture.title %>
            </h2>
            <p class="text-gray-600 text-base mb-4">
              <%= lecture.description %>
            </p>
            <a href="/lecture/<%= lecture._id %>"
              class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded transition duration-300">
              강의 보기
            </a>
          </div>
        </div>
        <% }) %>
    </div>
  </div>
</div>



<% function formatDuration(duration) { const hours=Math.floor(duration / 3600); const minutes=Math.floor((duration %
  3600) / 60); const seconds=duration % 60; return `${padZero(hours)}:${padZero(minutes)}:${padZero(seconds)}`; }
  function padZero(num) { return num.toString().padStart(2, '0' ); } %>